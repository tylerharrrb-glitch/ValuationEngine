# data

- `rates.seed.json`: registry seed bundled into the client (offline fallback).
- `rates.manual.json`: manually maintained, git-versioned entries merged over the Worker output by `functions/api/rates.ts`.
