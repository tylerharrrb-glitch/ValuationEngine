/** Placeholder shell while the v2 UI (spec Part 9) is rebuilt on the new engine. */
export default function App() {
  return (
    <main style={{ padding: 32, fontFamily: 'var(--font-ui)', color: 'var(--text)', background: 'var(--bg)', minHeight: '100vh' }}>
      <h1 style={{ fontFamily: 'var(--font-display)' }}>WOLF Valuation Engine</h1>
      <p>Version 2 interface in progress on branch rebuild/v2.</p>
    </main>
  );
}
